{{#each resources}}
  <section style="display:inline-block">
   <h3>{{this.title}}</h3>
   <a href={{this.url}}>Visit this resource</a>
   <a href='/create-review/{{this.endpoint}}'>Write review</a>
  </section>
{{/each}}
